import { Typography } from "@/registry/default/ui/typography";

export default function TypographyExtraSmall() {
  return (
    <Typography variant="xs">
      Minimal text for metadata, footnotes, or other supportive content.
    </Typography>
  );
}
