import { Typography } from "@/registry/default/ui/typography";

export default function TypographyRegularBOld() {
  return (
    <Typography variant="h6">
      A lightweight heading style used for supporting information, metadata, or
      minor section titles. Ideal for low-priority headers in UI or content
      hierarchy.
    </Typography>
  );
}
