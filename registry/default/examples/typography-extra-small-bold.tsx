import { Typography } from "@/registry/default/ui/typography";

export default function TypographyExtraSmallBold() {
  return (
    <Typography variant="xs" bold>
      Bold text for elements in tight spaces, buttons & Links
    </Typography>
  );
}
