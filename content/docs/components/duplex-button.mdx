---
title: DuplexButton
description: A versatile button component that changes its label, icon, and appearance based on active and hover states. Perfect for implementing follow/unfollow, like/unlike, or similar toggle interactions.
component: true
---

<ComponentPreview
  name="duplex-button-demo"
  description="DuplexButton example"
/>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add "https://github.com/AKASHAorg/akasha-ui/tree/main/public/r/styles/default/duplex-button.json"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install next
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="duplex-button" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import {
  DuplexButton,
  DuplexButtonActive,
  DuplexButtonHover,
  DuplexButtonInactive,
} from "<path-to-ui-components>/duplex-button";
```

```tsx
const [subscribe, setSubscribe] = useState(false);

<DuplexButton active={subscribe}>
  <DuplexButtonInactive
    variant="default"
    onClick={() => {
      setSubscribe(true);
    }}
  >
    Subscribe <UsersIcon className="h-4 w-4" />
  </DuplexButtonInactive>

  <DuplexButtonHover
    variant="outline"
    onClick={() => {
      setSubscribe(false);
    }}
  >
    Unsubscribe <UsersIcon className="h-4 w-4" />
  </DuplexButtonHover>

  <DuplexButtonActive>
    Subscribed <UserIcon className="h-4 w-4" />
  </DuplexButtonActive>
</DuplexButton>;
```
