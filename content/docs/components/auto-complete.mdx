---
title: AutoComplete
description: An autocomplete component.
component: true
---

## Single option selection

<ComponentPreview
  name="auto-complete-single-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="AutoComplete with selection of single option example"
/>

## Multiple options selection

<ComponentPreview
  name="auto-complete-multiple-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="AutoComplete with selection of multiple options example"
/>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add "https://github.com/AKASHAorg/akasha-ui/tree/main/public/r/styles/default/auto-complete.json"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install next
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="auto-complete" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { AutoComplete } from "@/components/akasha-ui/auto-complete";
```

```tsx
 <AutoComplete
  options={frameworks}
  placeholder="Select framework..."
  emptyMessage="No framework found."
  value={selected}
  onValueChange={(value) => setSelected(value)}
/>
```