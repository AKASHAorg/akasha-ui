---
title: Icon Container
description: A customizable container of icons.
component: true
---

<ComponentPreview name="icon-container-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<Step>Run the following command to install `button.tsx`</Step>

```bash
npx shadcn@latest add "https://akasha-ui.pages.dev/r/styles/default/icon-container.json"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="icon-container" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { IconContainer } from "<path-to-ui-components>/icon-container";
```

```tsx
<IconContainer>
  <Icon className="w-full h-full p-2" />
</IconContainer>
```

## Props

| Name                | Type                           | Description                                      |
| ------------------- | ------------------------------ | ------------------------------------------------ |
| variant             | `round` or `square`            | Component variants.                              |
| size                | `xs`, `sm`, `md`, `lg` or `xl` | Component sizes.                                 |
| showNotificationDot | `boolean`                      | Marks if a notification dot should be displayed. |

<Callout>

The **default** value of `variant` is `round` and that of `size` is `md`.

</Callout>

## Examples

### Round

<ComponentPreview name="icon-container-round" />

### Square

<ComponentPreview name="icon-container-square" />

### Show notification dot

<ComponentPreview name="icon-container-notification-dot" />
